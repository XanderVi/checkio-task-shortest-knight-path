def knight_path(cells: str) -> int:
    """
    Number of moves in the shortest path of knight
    """
    return 0

if __name__ == "__main__":
    #These "asserts" using only for self-checking and not necessary for auto-testing
    assert knight_path("b1-d5") == 2, "1st example"
    assert knight_path("a6-b8") == 1, "2nd example"
    assert knight_path("h1-g2") == 4, "3rd example"
    assert knight_path("h8-d7") == 3, "4th example"
    assert knight_path("a1-h8") == 6, "5th example"
